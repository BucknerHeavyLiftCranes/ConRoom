/** @type {string} path to server root (should use more specific paths). */
export const rootKey = `http://127.0.0.1:5173`

/** @type {string} path to server root (should use more specific paths). */
export const reservationKey = `http://127.0.0.1:5173/api/reservations`

/** @type {string} path to server root (should use more specific paths). */
export const roomKey = `http://127.0.0.1:5173/api/rooms`

// export const authorizationURL = `https://oauth2.example.com/authorize?response_type=code&client_id=${process.env.CLIENT_ID}&redirect_uri=${process.env.REDIRECT_URI}&state=${process.env.STATE}`;
// const tokenUrl = `https://login.microsoftonline.com/${process.env.CLIENT_ID}/oauth2/v2.0/token`;
// window.location.href = authorizationURL;